I find the following commands useful:

export AWS_ACCESS_KEY_ID=id
export AWS_SECRET_ACCESS_KEY=id

export AWS_ACCESS_KEY_ID=id && export AWS_SECRET_ACCESS_KEY=id && aws --region very-real-region dynamodb list-tables --endpoint-url http://localhost:8000

rm -rf .dynamodb && sls dynamodb install